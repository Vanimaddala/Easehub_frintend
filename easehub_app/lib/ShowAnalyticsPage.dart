import 'package:flutter/material.dart';

class ShowAnalyticsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Show Analytics'),
      ),
      body: Center(
        child: Text('Show Analytics Page'),
      ),
    );
  }
}
